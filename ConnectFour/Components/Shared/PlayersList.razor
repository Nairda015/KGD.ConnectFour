@using ConnectFour.Persistence


@foreach (var (conn, gameId) in Players.GetAll().Take(10))
{
    <PlayerRow ConnectionId="@conn" GameId="@gameId"/>
}

@code {

    [Inject]
    public required Lobby Players { get; set; }
}