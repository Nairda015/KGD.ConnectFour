@using ConnectFour.Persistence


@foreach (var (score, playerId, gameId) in Players.GetBestPlayers())
{
    <PlayerRow Score="@score" PlayerId="@playerId" GameId="@gameId"/>
}

@code {

    [Inject]
    public required PlayersContext Players { get; set; }
}